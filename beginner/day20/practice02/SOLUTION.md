# 解题结构

本文件不提供完整答案。对应的 `solution.ts` 只保留可通过类型检查的 TODO 脚手架，请先独立作答，再用这里检查思路。

## 方案一

1. 把每段 JSON 文本解析为 `unknown`。
2. 让课程守卫先确认非空对象，再检查标题和分数字段的运行时类型。
3. 守卫成功后才读取课程字段，否则沿无效数据路径展示提示。

## 关键检查点

- 不要用类型断言把外部数据直接当成 `Course`。

- 字段存在但类型错误时也必须判定为无效。
