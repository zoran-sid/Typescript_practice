# 解题结构

本文件不提供完整答案。对应的 `solution.ts` 只保留可通过类型检查的 TODO 脚手架，请先独立作答，再用这里检查思路。

## 方案一

1. 分别创建两个计数器实例，让输入分钟进入各自的私有状态。
2. 由实例方法根据当前主题与累计状态生成摘要。
3. 把其中一个实例交给 `Dashboard`，由接口能力生成汇总文字。

## 关键检查点

- 两个实例不能共享全局分钟数。

- 类外只通过公开方法读取结果，不直接访问私有字段。
