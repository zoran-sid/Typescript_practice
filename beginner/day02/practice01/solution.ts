// 这是教学脚手架，不是完整答案。请完成所有 TODO。
const completedText = "3";
const plannedLessons = 2;
const completed = 0; // TODO：用 Number(...) 转换 completedText。
const totalLessons = 0; // TODO：把转换后的数字与 plannedLessons 相加。
let status = "Keep learning";
const reachedGoal = false; // TODO：写出包含边界的比较表达式。
if (reachedGoal) {
  status = "Goal reached";
}
console.log(`Total lessons: ${totalLessons}`);
console.log(`Status: ${status}`);
