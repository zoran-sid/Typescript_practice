# Day 02 · Practice 02 解题结构提示

> 本文件不提供完整答案。`solution.ts` 的单价转换、总价计算和阈值判断仍需完成。

## 标准结构

先保留外部价格文本，再得到一个数字单价；数量本来就是数字，不需要重复转换。总价必须由这两个数字计算，随后由一个布尔比较决定是否更新默认状态。

## 自检

- 转换只包住价格文本，不要把整个运算放入转换调用。
- 总价使用乘法，不是字符串拼接。
- `if` 的条件来自总价与边界的比较。
- 输出来自总价变量和状态变量。