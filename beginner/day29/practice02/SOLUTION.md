# 解题结构

本文件不提供完整答案。`solution.ts` 是可通过类型检查的 TODO 脚手架，只展示主要声明顺序、类型边界和待完成位置。

## 方案一：用四个小型类型工具练习派生关系

### 搭建顺序

1. 以 `Settings` 和只读主题元组作为源。
2. 分别保留 Flags、数组元素提取、处理器名称和 Promise 解包的类型骨架。
3. 创建最少量测试值，让编译器确认键名、字面量和解包结果。

### 类型与数据流

对象/数组/字符串/Promise 类型 → 映射或条件类型 → 具体派生类型 → 测试值。

### 关键自检点

- 避免手写已经存在于源类型中的键或联合成员。
- 条件类型的非匹配分支也要符合题目希望的语义。

完成 TODO 后，请以本题 README 的精确输出和限制逐项自检；不要把显示结果直接写死。
