# 解题结构

本文件不提供完整答案。对应的 `solution.ts` 只保留可通过类型检查的 TODO 脚手架，请先独立作答，再用这里检查思路。

## 方案一

1. 让文本先进入解析函数，把格式或范围失败留给异常通道。
2. 把已解析端口交给保存函数，用 `Result` 表达预期的业务成功或失败。
3. 调用处先处理异常，再按 `ok` 收窄保存结果并展示对应信息。

## 关键检查点

- `catch` 中的值保持 `unknown`，收窄后才能读取消息。

- 不要把解析异常和正常业务失败混成同一种返回路径。
