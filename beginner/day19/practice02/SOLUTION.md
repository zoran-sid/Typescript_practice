# 解题结构

本文件不提供完整答案。对应的 `solution.ts` 只保留可通过类型检查的 TODO 脚手架，请先独立作答，再用这里检查思路。

## 方案一

1. 让每段文本先经过数值转换与端口合法性检查。
2. 在调用处用 `try/catch` 分开合法端口与解析失败。
3. 再把合法端口交给保存函数，并通过判别字段读取结果。

## 关键检查点

- 验证要同时覆盖非数字、整数和范围。

- 失败时抛标准错误对象，不要抛字符串。
