# 解题结构

本文件不提供完整答案。`solution.ts` 是可通过类型检查的 TODO 脚手架，只展示主要声明顺序、类型边界和待完成位置。

## 方案一：让搜索、请求和命令行逻辑可注入

### 搭建顺序

1. 保留搜索回调、JSON 客户端和参数解析函数的签名。
2. 在浏览器入口只绑定事件，把读到的查询交给回调。
3. 请求与 CLI 分别完成 unknown 验证和缺失值检查，再返回普通类型。

### 类型与数据流

DOM/客户端/参数数组 → 适配函数 → `string`、标题或 day → 调用者。

### 关键自检点

- 核心逻辑不要直接读取全局 DOM、fetch 或 process。
- 数字转换后还要检查结果是否满足题目范围。

完成 TODO 后，请以本题 README 的精确输出和限制逐项自检；不要把显示结果直接写死。
