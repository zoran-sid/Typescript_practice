# Day 27 参考思路

完整答案在 `solution.ts`。

- `InputEventLike` 只描述业务真正需要的事件形状，空目标由可选链与空值合并处理。
- `loadLesson` 在 await 后逐字段验证 unknown，坏响应得到 `null`。
- `parseArgs` 不读取全局环境；标志后的值先按可能缺失处理，再转换和检查。

## 拓展思考方向

网页入口只需查找元素、注册监听器、创建调用 fetch 的客户端，再把值交给这些普通函数。纯函数不依赖 DOM 和网络，因此能用固定输入快速测试，真实环境代码保持很薄。
