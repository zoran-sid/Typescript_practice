# 解题结构

本文件不提供完整答案。对应的 `solution.ts` 只保留可通过类型检查的 TODO 脚手架，请先独立作答，再用这里检查思路。

## 方案一

1. 让每个 `LoadState` 从数组进入状态描述函数。
2. 用 `status` 收窄后，在成功分支处理项目数组，在失败分支处理错误文字。
3. 由外层循环依次展示每次描述函数交回的字符串。

## 关键检查点

- 每个分支只访问该联合成员拥有的字段。

- 四种状态都要进入对应路径，不能用一条固定文字代替。
