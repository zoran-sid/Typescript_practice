# 解题结构

本文件不提供完整答案。`solution.ts` 是可通过类型检查的 TODO 脚手架，只展示主要声明顺序、类型边界和待完成位置。

## 方案一：用元组与泛型保存高级函数的对应关系

### 搭建顺序

1. 先保留进度元组和进度函数，再搭建 `invoke` 的参数元组与返回类型骨架。
2. 为 normalize 写公开重载，联合实现只负责运行时分支。
3. 最后声明显式 `this` 的描述函数，并通过指定上下文调用。

### 类型与数据流

实参元组 → 泛型调用 → 原返回类型；联合输入 → 重载选择 → 精确调用结果。

### 关键自检点

- 元组的位置和含义不能互换，rest 参数必须连接到同一个参数列表类型。
- 显式 `this` 不计入普通实参数量。

完成 TODO 后，请以本题 README 的精确输出和限制逐项自检；不要把显示结果直接写死。
