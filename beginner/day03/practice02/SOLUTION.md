# Day 03 · Practice 02 解题结构提示

> 本文件不提供完整答案。`solution.ts` 只保留温度数组、统计变量和控制流骨架。

## 标准结构

做法与学习时长统计相同，但变量名称必须反映温度业务：循环变量代表本轮读数，总和变量跨轮累计，最高值只在当前读数更高时改变。

## 自检

- 每个数组元素只处理一次。
- 总和不会在每轮被当前温度覆盖。
- 最高值不是无条件更新成最后一项。
- 输出数量来自数组，另两项来自循环后的统计变量。

拓展到可能全为负数的数据时，不要盲目用 0 初始化最高值；先考虑数组是否保证非空，再决定是否从第一项初始化。