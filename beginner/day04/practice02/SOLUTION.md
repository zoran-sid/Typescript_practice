# Day 04 · Practice 02 解题结构提示

> 本文件不提供完整答案。年龄转换、四个票价分支和入场条件仍在 `solution.ts` 中标为 `TODO`。

## 标准结构

先把年龄文本转换为数字，再按互斥优先级确定一个票价。每个年龄只能进入整条分支链中的一个分支。票价确定后，再用独立的布尔表达式判断能否独自入场。

## 自检

- 所有年龄比较都使用转换后的数字。
- 学生条件同时考虑年龄与学生证。
- 各分支最终让 `ticketPrice` 得到数字。
- `canEnterAlone` 是布尔值，不是显示文字。
- 测试边界年龄时，确认不会落入相邻两个规则。