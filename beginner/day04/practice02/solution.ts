// 这是教学脚手架，不是完整答案。请完成所有 TODO。
const ageText = "20";
const age = 0; // TODO：把年龄文本转换成数字。
const hasStudentCard = true;
let ticketPrice = 0;
if (false) { // TODO：替换成儿童条件。
  // TODO：设置儿童票价。
} else if (false) { // TODO：替换成老年条件。
  // TODO：设置老年票价。
} else if (false) { // TODO：替换成需要组合两个条件的学生条件。
  // TODO：设置学生票价。
} else {
  // TODO：设置普通票价。
}
const canEnterAlone = false; // TODO：组合年龄与有效票价条件。
console.log(`年龄: ${age}`);
console.log(`票价: ${ticketPrice}`);
console.log(`允许独自入场: ${canEnterAlone}`);
