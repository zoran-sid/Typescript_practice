// 这是解题结构，不是完整答案。TODO 旁的空字符串、0、false、[] 等只是占位值，完成时要替换或删除。
const productName: string = "Keyboard";
let stock: number = 5;
const isTracked: boolean = true;

const beforeStock = 0; // TODO：在修改 stock 之前，把当前库存保存成一份数字快照。

// TODO：发生了两次出库。每次让 stock 在原值基础上减少 1，并把新值存回 stock。

const productLine = ""; // TODO：使用 productName 生成“商品: ...”。
const beforeLine = ""; // TODO：使用 beforeStock 生成“出库前库存: ...”。
const afterLine = ""; // TODO：使用修改后的 stock 生成“出库后库存: ...”。
const trackingLine = ""; // TODO：使用 isTracked 生成“启用库存跟踪: ...”。

console.log(productLine);
console.log(beforeLine);
console.log(afterLine);
console.log(trackingLine);
