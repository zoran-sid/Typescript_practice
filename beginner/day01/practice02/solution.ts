// 这是解题结构，不是完整答案。TODO 旁的空字符串、0、false、[] 等只是占位值，完成时要替换或删除。
const learnerName: string = "Ada";
let completedLessons: number = 0;
const isBeginner: boolean = true;
// TODO：读取 completedLessons 当前的 0，加 1 后赋值回 completedLessons，得到 Ada 的最新完成课数。
const firstSummary = ""; // TODO：空字符串只是 string 类型占位；用 learnerName、更新后的 completedLessons 和 isBeginner 组合 Ada 的完整摘要，并保存到 firstSummary。
const studentName: string = "Zoran";
let completedLesson: number = 1;
const isBeginner2: boolean = true;
// TODO：读取 completedLesson 当前的 1，加 1 后赋值回 completedLesson，得到 Zoran 的最新完成课数。
const secondSummary = ""; // TODO：空字符串只是 string 类型占位；用 studentName、更新后的 completedLesson 和 isBeginner2 组合第二条摘要，并保存到 secondSummary。
console.log(firstSummary);
console.log(secondSummary);
