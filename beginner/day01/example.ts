const learnerName: string = "Ada";
let completedLessons: number = 0;
const isBeginner: boolean = true;

completedLessons = completedLessons + 1;

const summary = `${learnerName} completed ${completedLessons} lesson. Beginner: ${isBeginner}`;

console.log(summary);

const studentname = "Zoran";
let completedLesson = 1;
const isBeginner2  = true;

completedLesson = completedLesson + 1;

const summary2 =  `${studentname} completed ${completedLesson} and Lesson Beginner is: ${isBeginner2}`;

console.log(summary2);