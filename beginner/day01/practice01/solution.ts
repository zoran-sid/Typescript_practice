// 这是解题结构，不是完整答案。TODO 旁的空字符串、0、false、[] 等只是占位值，完成时要替换或删除。
const learnerName = "Lin";
const courseName = "TypeScript";
let completedLessons = 0;
const isBeginner = true;
// TODO：completedLessons 当前为 0；每次读取它的旧值并加 1，连续赋值两次，让最终值保存回 completedLessons。
// completedLessons = ...
// completedLessons = ...
// TODO：分别读取 learnerName、courseName、最终的 completedLessons 和 isBeginner，用模板字符串生成题目要求的四行输出。
// console.log(...)
