# 解题结构

本文件不提供完整答案。对应的 `solution.ts` 只保留可通过类型检查的 TODO 脚手架，请先独立作答，再用这里检查思路。

## 方案一

1. 从 `original` 进入更新函数，并找出外层、数组、嵌套对象和目标任务四条变化路径。
2. 沿每条变化路径创建新容器，只覆盖题目要求改变的字段。
3. 解构 `updated.skills` 后，同时读取新旧对象来验证原数据未被修改。

## 关键检查点

- 只复制最外层仍会共享嵌套容器。

- 不要用 `push`、直接赋值或其他原地修改绕过不可变更新。
