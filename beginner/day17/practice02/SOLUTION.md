# 解题结构

本文件不提供完整答案。对应的 `solution.ts` 只保留可通过类型检查的 TODO 脚手架，请先独立作答，再用这里检查思路。

## 方案一

1. 从文章类型派生预览字段和允许修改的补丁字段。
2. 让更新函数接收原文章与补丁并返回新对象。
3. 从只读状态列表派生状态联合，再让标签映射接受类型检查。

## 关键检查点

- 不要重复手写一份容易漂移的状态联合。

- 补丁类型只开放题目允许改变的字段。
