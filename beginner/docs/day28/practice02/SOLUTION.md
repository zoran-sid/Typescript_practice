# 解题结构

[返回题目](./README.md) · [打开 solution.ts](../../../day28/practice02/solution.ts)

本文件不提供完整答案。`solution.ts` 是可通过类型检查的 TODO 脚手架，只展示主要声明顺序、类型边界和待完成位置。

## 方案一：先写调用契约，再补联合实现

### 搭建顺序

1. 声明课程元组并在参数位置解构。
2. 让 `invoke` 同时保存函数参数列表与返回值的关系。
3. 为 normalize 保留两条调用签名，再在实现中收窄；独立验证显式 `this`。

### 类型与数据流

元组/函数/上下文 → 类型契约 → 运行时分支或调用 → 保持精确的结果类型。

### 关键自检点

- 调用签名面向使用者，实现签名不能泄露过宽结果。
- 不要用 `any[]` 切断参数元组与原函数之间的关系。

完成 TODO 后，请以本题 README 的精确输出和限制逐项自检；不要把显示结果直接写死。
