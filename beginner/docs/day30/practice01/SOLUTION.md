# 解题结构

[返回题目](./README.md) · [打开 solution.ts](../../../day30/practice01/solution.ts)

本文件不提供完整答案。`solution.ts` 是可通过类型检查的 TODO 脚手架，只展示主要声明顺序、类型边界和待完成位置。

## 方案一：让声明文件忠实连接旧 JavaScript 与现代入口

### 搭建顺序

1. 先阅读旧模块真实导出，再在 `.d.ts` 中只描述实际存在的参数和返回值。
2. 利用接口合并组成课程信息，保留旧状态与现代状态之间的规范化函数。
3. 入口文件导入运行时实现，并用声明提供的类型检查完成组合。

### 类型与数据流

旧 JS 实现 + `.d.ts` 契约 → TypeScript 入口 → 规范化值 → 输出。

### 关键自检点

- 声明文件不会创建实现，不能声明一个运行时不存在的导出。
- 业务规则变化若不改变类型形状，仍需要运行时测试保护。

完成 TODO 后，请以本题 README 的精确输出和限制逐项自检；不要把显示结果直接写死。
