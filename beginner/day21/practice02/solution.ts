// 解题结构提示：本文件不是完整答案，请沿 TODO 自己补全。
async function fetchText(name: string, shouldFail = false): Promise<string> {
  await Promise.resolve();
  // TODO：实现失败分支与成功返回；当前空字符串只是安全占位。
  return "";
}
async function main(): Promise<void> {
  // TODO：用 Promise.all 并行等待课程与进度。
  // TODO：用 try/catch 等待失败请求，catch 参数保持 unknown。
  await Promise.resolve();
}
await main();
