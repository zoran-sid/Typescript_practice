# 解题结构

本文件不提供完整答案。对应的 `solution.ts` 只保留可通过类型检查的 TODO 脚手架，请先独立作答，再用这里检查思路。

## 方案一

1. 让 `fetchText` 根据参数建立成功与失败两条异步路径。
2. 在 `main` 中先同时启动课程与进度请求，再统一等待组合结果。
3. 对失败请求单独等待并捕获，收窄错误后再展示消息。

## 关键检查点

- 并行请求应在等待前全部创建。

- 失败 Promise 必须被观察，不能留下未处理拒绝。
